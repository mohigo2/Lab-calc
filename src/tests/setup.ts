// Jest setup file for Buffer Calc plugin tests

// Global test configuration
beforeEach(() => {
  // Clear console logs between tests
  jest.clearAllMocks();
});

// Test setup - no additional mocks needed here as they're handled in jest.config.js
export {};